export default class Location{
    constructor(uri, description, size, height, width, format){
        this.uri = uri;
        this.description = description;
        this.size = size;
        this.height = height;
        this.width = width;
        this.format = format;
    }
}