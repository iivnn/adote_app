export default class Location{
    constructor(cep, adress, number, city, district, state){
        this.cep = cep;
        this.adress = adress;
        this.number = number;
        this.city = city;
        this.district = district;
        this.state = state;
    }
}